# FizzBits sample web application

This is a sample web application that takes information from
a deployed iOS application and logs the JSON package that is
sent into the database (checking for duplicates).

